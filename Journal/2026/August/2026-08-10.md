---
date: 2026-08-10
level: "5"
xp_before: 750
xp_after: 920
status: complete
tags:
  - daily-quest
---

# Daily Quest Log — 2026-08-10

## Character Status

**Level:**  5
**XP Before:**  750
**XP After:**  920

## Main Quest
## ⚙ Main Quest — Build the Safe Proving Grounds

**Quest_id:** `2026-08-10-main-safe-json-lab`  
**Path:** Senior DevOps/SRE  
**Type:** Guided Lab / Infrastructure Quest  
**Estimated Time:** Standard — 30–45 min  
**Expected XP:** **100 XP**
**Status:** **Complete**
### Objective
**Objective:** Learn how to deliberately test valid and malformed JSON in a disposable environment without touching the real Arcane Engineer event pipeline.

**Prerequisites:** VS Code for creating/managing the lab files, and PowerShell for executing the test script. You do **not** need to understand the Guild's complete event engine yet.

**Safety Boundary:** Do not modify `System/Event-Drop.json`, do not place these training events in `System/Inbox`, and do not run `Import-SystemEvent.ps1`. Nothing in this lab should modify `Data/character-state.json`.

In VS Code, create:

```
Labs/
└── JSON-Validation/
    ├── valid-event.json
    ├── malformed-event.json
    └── Test-Json.ps1
```

Put this into `valid-event.json`:

```
{
  "event_id": "training-event-001",
  "quest_name": "Training Event",
  "xp": 10
}
```

Put this deliberately broken JSON into `malformed-event.json`:

```
{
  "event_id": "training-event-002",
  "quest_name": "Broken Training Event",
  "xp": 10,
}
```

Create `Test-Json.ps1`:

```
param(
    [Parameter(Mandatory)]
    [string]$Path
)

Write-Host "`nTesting: $Path" -ForegroundColor Cyan

try {
    $content = Get-Content $Path -Raw
    $null = $content | ConvertFrom-Json -ErrorAction Stop

    Write-Host "VALID JSON" -ForegroundColor Green
}
catch {
    Write-Host "INVALID JSON" -ForegroundColor Red
    Write-Host $_.Exception.Message -ForegroundColor Yellow
}
```

From the repository root, test the valid file:

```
.\Labs\JSON-Validation\Test-Json.ps1 `
    -Path .\Labs\JSON-Validation\valid-event.json
```

**Expected result:**

```
VALID JSON
```

Before running the malformed test, stop here and complete today's Intellect Trial.

Then execute:

```
.\Labs\JSON-Validation\Test-Json.ps1 `
    -Path .\Labs\JSON-Validation\malformed-event.json
```

**Expected result:**

```
INVALID JSON
```

The exact error text may vary. That is okay. What matters is that the script enters `catch` instead of treating the input as valid.

**Verification:** In VS Code Source Control, inspect the changed files. The lab files should be new, but your real character/event files should not have changed merely because you executed these tests.

In particular, you should see no test-caused modification to:

```
System/Event-Drop.json
Data/character-state.json
Character/Character-Sheet.md
Guild-Hall.md
```
**Evidence required:** `Test-Json.ps1`, both JSON files, the VALID and INVALID results in your journal or screenshots, and 2–3 sentences explaining why this lab cannot alter your canonical character state.

**Optional Stretch Goal — 0 required XP:** Add a third `empty-event.json` containing `{}` and predict whether the current script considers it valid. This begins demonstrating the difference between **valid JSON syntax** and **valid SYSTEM EVENT data**.
### Evidence
This is provided in two locations: here in this note and in the latest PR: 

"2–3 sentences explaining why this lab cannot alter your canonical character state.":
I'm not entirely sure how to answer this question. It is my understanding that if nothing is added to the "Event-Drop.json" then no new data will be added to the character state.
### What I Did
I added the Labs folder with the subfolder and files in this DIR ".\Projects\DevOps\Labs\JSON-Validation\". I ran the script on valid-event.json, stopped and completed the Intellect Trial, then, I ran the script again with the malformed-event.json.
### Guild Master Feedback
## ⚙ Main Quest — Approved, 100 XP

You created the isolated lab under `Projects/DevOps/Labs/JSON-Validation/`, which is actually a better organizational location than the originally suggested repository-root `Labs/`. The PR contains the PowerShell validator and both test inputs.

Your safety explanation is basically correct, but here's the deeper answer I want you to take away:

> The lab cannot alter canonical character state because `Test-Json.ps1` only **reads** the supplied JSON file and passes its contents to `ConvertFrom-Json`. It never calls the Guild importer, never writes to `System/Event-Drop.json`, and never writes to `Data/character-state.json`.

That's the important engineering distinction: **not merely that you didn't put anything into Event-Drop, but that the test program has no code path capable of mutating the Guild state.**

Full reward approved.
## Lore Quest
## 📖 Lore Quest — Understanding PowerShell Error Handling

**quest_id:** `2026-08-10-lore-powershell-errors`  
**Path:** Senior DevOps/SRE  
**Type:** Micro Quest  
**Estimated Time:** Micro — 10–15 min  
**Expected XP:** **25 XP**
**Status:** **Complete**
### Reading or Resource
**Objective:** Learn what the Main Quest's `try`, `catch`, and `-ErrorAction Stop` are doing rather than simply copying the script.

Study the PowerShell concepts of `try {}`, `catch {}`, terminating errors, and `-ErrorAction Stop`.

Then answer in today's Adventurer Journal:

```
1. What code belongs inside try?

2. Under what condition does catch execute?

3. Why does this script use -ErrorAction Stop?

4. Give one example of where this kind of error handling
   could protect a DevOps automation pipeline.
```

**Evidence required:** Your four answers in the journal. They can be short; demonstrating understanding matters more than length.
### Lesson Learned
1. What code belongs inside try? Anything you want to run once or try running.

2. Under what condition does catch execute? Any time the try is not in a successful output or any errors occur.

3. Why does this script use -ErrorAction Stop? The "ErrorAction" flag is used for error handling and the Stop value, is to stop the script or terminate the script run. The reason for this script to run this is, can not parse on a null value or at least that is my current read on this.

4. Give one example of where this kind of error handling
   could protect a DevOps automation pipeline. When you are trying to run a script locally to check the health of the agent and the script fails to can receive the error and disable the machine in the pool before the agent try's to do any more work.
### Guild Master Feedback
## 📖 Lore Quest — Approved, 25 XP

Your understanding is developing correctly, with two corrections worth learning.

`try` contains code that **might throw an error that you want to handle**. `catch` does not necessarily run whenever something is "unsuccessful"; it runs when a **terminating error** occurs in the associated `try`. `-ErrorAction Stop` is important because it can promote an otherwise non-terminating PowerShell error into a terminating error that `catch` can handle.

Your agent-health example is excellent DevOps thinking: detect failure → handle it → prevent an unhealthy agent from receiving more work.

Full reward approved.
## Intellect Trial
## 🧠 Intellect Trial — Predict Before Executing

**quest_id:** `2026-08-10-intellect-json-prediction`  
**Path:** Senior DevOps/SRE  
**Type:** Micro Quest  
**Estimated Time:** Micro — 5–10 min  
**Expected XP:** **20 XP**
**Status:** **Complete**
### Challenge
**Objective:** Practice the engineering cycle:

**Hypothesis → Controlled Experiment → Observation → Conclusion**

Before running `malformed-event.json`, create this section in today's journal:

```
### My Prediction

Will PowerShell stop completely?

Will catch execute?

Will malformed-event.json change?

Will anything be deleted?

Will Data/character-state.json change?
```

Write your predictions without looking up the answers.

Then execute the malformed test.

Add:

```
### What Actually Happened

...

### Difference Between My Prediction and Reality

...
```

Being wrong does **not** reduce the reward. A useful experiment that disproves your hypothesis is successful engineering.

**Evidence required:** Prediction, actual result, and at least one comparison between the two.
### My Prediction

Will PowerShell stop completely? No, it will run the try, come across the error then, catch will run

Will catch execute? Yes

Will malformed-event.json change?  No

Will anything be deleted? No

Will Data/character-state.json change? No

### What Actually Happened
INVALID JSON
Invalid JSON primitive: .
### Difference Between My Prediction and Reality
Not knowing what would be printed in the console "Invalid JSON primitive: ."
### Guild Master Feedback
## 🧠 Intellect Trial — Approved, 20 XP

Your hypothesis was strong. You correctly predicted that `catch` would execute, the malformed input would remain unchanged, nothing would be deleted, and character state would remain unchanged.

Most importantly, you **predicted before executing**. That's the behavior this trial was intended to develop.

Full reward approved.
## Charisma Challenge
## 🗣 Charisma Challenge — Explain the Safety Boundary

**quest_id:** `2026-08-10-charisma-explain-safe-lab`  
**Path:** Senior DevOps/SRE  
**Type:** Micro Quest  
**Estimated Time:** Micro — 5–10 min  
**Expected XP:** **20 XP**
**Status:** **Not Attempted**
### Topic
**Objective:** Imagine another engineer asks:

> Why didn't you just put malformed JSON into the real Arcane Engineer importer and see what happened?

Write a **3–5 sentence technical explanation**.

Try to communicate this progression:

```
Unknown behavior
      ↓
Controlled environment
      ↓
Experiment
      ↓
Understanding
      ↓
Confidence
      ↓
Production-like testing
```

Avoid simply saying, "because it might break."

Explain what isolation gives the engineer.

**Evidence required:** Your finished explanation in today's Adventurer Journal.
### Audience

### My Explanation

### Guild Master Feedback

### Reward

## Creativity Quest
## 🎨 Creativity Quest — Build the Portfolio Lab README

**quest_id:** `2026-08-10-creativity-json-lab-readme`  
**Path:** Senior DevOps/SRE  
**Type:** Portfolio Artifact  
**Estimated Time:** Standard — 20–30 min  
**Expected XP:** **50 XP**
**Status:** **Not Attempted**

**Path:**

- [ ] The Wizard Who Crochets
- [ ] Senior DevOps/SRE

### Objective
**Objective:** Turn today's learning exercise into something another engineer could understand from your repository.

Create:

```
Labs/
└── JSON-Validation/
    └── README.md
```

Use this structure:

```
# PowerShell JSON Validation Lab

## Purpose

Explain what problem this experiment investigates.

## What I Built

Explain the small PowerShell validator.

## Lab Files

Explain:
- valid-event.json
- malformed-event.json
- Test-Json.ps1

## Valid JSON Test

Command used:

Result:

What the result means:

## Invalid JSON Test

Command used:

Result:

What the result means:

## What I Learned

Explain try/catch, ConvertFrom-Json, and
-ErrorAction Stop in your own words.

## Safety

Explain why this experiment cannot alter the
Arcane Engineer Guild character state.

## Why This Matters in DevOps

Explain why validating data before allowing it
into later automation stages is useful.
```

If you capture screenshots, you may store them with the project and embed them in the README or your Adventurer Journal.

**Evidence required:** `Labs/JSON-Validation/README.md` containing your actual test results and explanation.
### Evidence

### How This Advances My Goal

### Guild Master Feedback

### Reward

## Micro-Quest
## 🔐 DevOps Micro-Quest — Seal the Main Branch

**Category:** DevOps / GitHub / Repository Security  
**Type:** Micro Quest
**Reward:** 25 XP  
**Status:** **Complete**

### Objective

Protect the Arcane Engineer Guild repository's `main` branch from unauthorized or accidental direct changes by implementing a GitHub repository ruleset.

### Work Completed

Configured a GitHub branch ruleset targeting the repository's **default branch (`main`)**.

The protection establishes a controlled contribution workflow:

```text
Working Branch / Fork
        ↓
      Commit
        ↓
       Push
        ↓
   Pull Request
        ↓
      Review
        ↓
       Merge
        ↓
       main
```

The purpose of the ruleset is to prevent contributors from directly pushing unwanted code into `main`. Changes must instead be submitted through the Pull Request workflow so they can be reviewed before becoming part of the Guild's canonical repository state.

### Configuration

- Targeted the repository's **default branch**
    
- Used **Include default branch** rather than hard-coding the `main` branch name
    
- Enabled Pull Request protection for changes entering `main`
    
- Established PR review as the controlled path for updating the default branch
    
- Prevented normal contributors from bypassing the intended review workflow
    
- Preserved the ability for contributors to work through branches or forks and submit Pull Requests
    

### What I Learned

I learned the distinction between:

- `origin` — the remote GitHub repository
    
- `main` — the repository's default branch
    
- Working branches — branches where development can occur without modifying the protected branch
    
- Pull Requests — the review gate used to introduce changes into `main`
    
- GitHub Rulesets — repository controls that enforce how protected branches may be modified
    

I also learned that targeting **Include default branch** makes the protection follow the repository's designated default branch rather than depending on a hard-coded branch name.

### DevOps Significance

This implements a real-world source-control protection commonly used in professional software engineering and DevOps environments.

Rather than trusting developers to remember not to push directly to the primary branch, the repository itself enforces the policy.

This follows the principle:

> **Enforce important safety boundaries through tooling rather than relying solely on human behavior.**

### Evidence for Guild Master Review

**Repository:** Arcane-Engineer-Guild  
**Protected branch:** `main`  
**Protection mechanism:** GitHub Ruleset  
**Required workflow:** Branch/Fork → Pull Request → Review → Merge → `main`

### Reflection

This change moves the Arcane Engineer Guild repository closer to a production-style development workflow. The `main` branch represents trusted Guild state, while development occurs outside that protected boundary.

Future repository automation, such as JSON validation and GitHub Actions checks, can be added to this Pull Request gate so invalid changes are automatically rejected before they reach `main`.

### Reward Requested

**DevOps Micro-Quest: 25 XP**

**Guild Master Review:** Approved
### Guild Master Feedback
## 🔐 Bonus Micro-Quest — Approved, 25 XP

I'm also accepting **Seal the Main Branch**.

You implemented a meaningful DevOps control rather than simply documenting one: protecting the default branch and establishing the branch → PR → review → merge workflow. Your reflection also demonstrates that you understand the distinction between the remote repository, default branch, development branches, PR review, and rulesets.

That is legitimate Senior DevOps/SRE-path progress.
## Daily Rewards

**Total XP Earned:**  170

**Stats Increased:**  3

**Skills Improved:**  3

**Achievements Unlocked:**  Infrastructure Engineer progress

**Loot Acquired:**  no loot